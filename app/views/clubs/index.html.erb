<div class="pt-8">
  <h1 class="text-2xl font-bold text-gray-900 mb-6"><%= t('clubs.index.title') %></h1>

  <div class="mb-8">
    <button type="button" onclick="document.getElementById('new-club-form').classList.toggle('hidden')" class="btn-primary">
      <%= t('clubs.index.create_club') %>
    </button>
  </div>

  <div id="new-club-form" class="hidden bg-white dark:bg-neutral-800 border border-black/10 dark:border-white/10 rounded-xl shadow-sm p-6 mb-8 max-w-md">
    <%= form_with model: Club.new, url: clubs_path, local: true do |f| %>
      <div class="space-y-5">
        <div class="relative gap-y-1.5">
          <%= f.label :name, class: "form-label" %>
          <%= f.text_field :name, class: "form-control", required: true %>
        </div>
        <div class="relative gap-y-1.5">
          <%= f.label :sport, class: "form-label" %>
          <%= f.select :sport, [[t('clubs.index.run'), "run"], [t('clubs.index.bike'), "bike"], [t('clubs.index.soccer'), "soccer"]], { include_blank: t('clubs.index.select_sport') }, class: "form-control" %>
        </div>
        <div class="relative gap-y-1.5">
          <%= f.label :description, class: "form-label" %>
          <%= f.text_area :description, rows: 3, class: "form-control" %>
        </div>
        <div class="pt-2">
          <%= f.submit t('common.buttons.create'), class: "btn-primary w-full" %>
        </div>
      </div>
    <% end %>
  </div>

  <div class="space-y-4">
    <% @clubs.each do |club| %>
      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 flex justify-between items-center">
        <div>
          <h2 class="text-lg font-semibold text-gray-900"><%= club.name %></h2>
          <p class="text-sm text-gray-600 mt-1"><%= club.description %></p>
          <div class="flex gap-4 mt-2 text-sm text-gray-500">
            <span><%= club.sport&.capitalize %></span>
            <span><%= club.member_count %> <%= t('clubs.index.members') %></span>
          </div>
        </div>
        <% membership = current_user.club_memberships.find_by(club: club) %>
        <% if membership %>
          <%= link_to t('clubs.index.view'), club_path(club), class: "text-[#FC4C02] hover:underline font-medium" %>
        <% else %>
          <%= button_to t('clubs.index.join'), join_club_path(club), method: :post, class: "btn-primary" %>
        <% end %>
      </div>
    <% end %>
  </div>

  <% if @clubs.empty? %>
    <p class="text-gray-500 py-8"><%= t('clubs.index.no_clubs') %></p>
  <% end %>
</div>
